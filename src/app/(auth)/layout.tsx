/**
 * Fichier : src/app/(auth)/layout.tsx
 * Rôle : Layout pour les pages d'authentification
 */

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}